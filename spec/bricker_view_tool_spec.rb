require "spec_helper"

describe BrickerViewTool do
  it "has a version number" do
    expect(BrickerViewTool::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
